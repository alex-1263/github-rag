# 项目提案:GitHub Issue 语义知识库(A)与生态故障经验库(B)

> 评估日期:2026-09-19。定位:两个均为开源项目,不做商业化。
> 本文浓缩了多轮调研与设计讨论,是自包含的评估输入。

## 一、背景与问题

1. AI 编码普及导致开源项目 issue 爆炸式增长:数量爆炸(写 issue 门槛消失)、重复爆炸(用户用相似 prompt 让 AI 报 bug,产出高度趋同)、质量爆炸(AI 生成的幻觉性 bug 报告)、对话污染(评论区 AI 文本淹没)。维护者 triage 能力已达物理上限,部分项目直接关闭 issue 区。
2. GitHub 官方 2026 年 6-8 月连续出手:duplicate detection(创建时提示最多 3 条相似 issue,public preview)、"Relates to" issue 关系、agent automation controls(限流)、issue fields GA。官方做的是**创建时刻查重 + 限流**,不做存量消化、不做跨仓库、不做语义检索。
3. issue 库本质是三份未被利用的资产:**用户声音**(需求原话)、**机构记忆**(决策史/被否决的方案)、**故障史**(每个 bug 的根因与修复)。当前只能关键词搜索,语义不可检索。

## 二、项目 A:issue 语义知识库

**定位**:给多仓库维护者的语义记忆层;跨仓库语义检索 + 主题聚类 + agent 消费接口。

**核心功能**:
- 跨仓库语义检索("之前有没有人提过",官方功能全部单仓库,此为结构空白)
- 主题聚类 → 需求全景(2 万条 issue → 几百个主题,阅读负担 O(n) → O(主题数))
- 维护者晨报(昨晚新增 → 聚成主题 → AI 噪音标记 → 批量决策建议)
- MCP 工具集:需求上下文包(开工前召回相关历史 issue)、历史决策雷达(编码中防止 AI 无知推翻历史决策)、PR 语义校验(修复是否覆盖 issue 报告的全部场景)

**架构**(全部成熟组件,零现金成本可行):
GraphQL API 增量同步(按 updated_at,lazy catch-up ~10 分钟新鲜度)→ embedding(bge-m3 本地 CPU 或 OpenAI 3-small,embedding 是纯函数保证多端一致)→ sqlite-vec 单文件索引(1 万条 issue ≈ 40MB)→ 混合检索(向量 + BM25)→ base+delta 分发(每日 CI 全量基线发 GitHub Release 走 CDN + 本地增量)→ MCP server 消费(Claude/Copilot 等 agent 直接调用)。

**已识别风险**:
- 官方可能吸收功能面(开源定位下不致命,但压制社区采用)
- 聚类质量是核心资产,做不好一切归零
- 小众市场:被 2 万条 issue 淹没的头部维护者全球估计仅数千人
- 企业场景需 ACL 权限过滤 + Jira 等数据源适配(schema 第一天预留)

## 三、项目 B:生态故障经验库(bugcards)

**定位**:社区共建的生态级 bug 经验知识库。价值假设:大模型预训练虽已"读过"公开 issue,但参数化知识在长尾覆盖、溯源、新鲜度、版本区分上全面劣于检索;CVE/OSV 太窄(仅安全漏洞)、Stack Overflow 太泛,「生态级已知坑」(带复现+修复 diff 的工程经验)是结构化空白。

**形态**:markdown 卡片仓库。一个 bug 一个文件,frontmatter 结构化字段(症状/根因/修复 PR/影响版本/技术栈标签/溯源链接/reviewed 标记)+ 正文叙事。

**生产管道**(AI 自动化 + 社区审核):
label:bug 且有关联修复 PR 的 issue → 组装输入(正文 + 按信息密度过滤的评论 + PR diff)→ LLM 结构化抽取 → 质量关卡(入口狠过滤 / "放弃抽取"为合法输出 / 双模型交叉验证 / 抽检监控)→ `reviewed: false` 初稿入库 → 社区 PR 审核翻 `true`。
诚实天花板:全自动约 80-90 分质量,最后 10 分靠人——这恰好是社区共建的参与接口(审卡片比写卡片门槛低一个数量级)。

**分发**(双通道,CI 焊接):
- 路线 1:git clone 文档仓库 → 本地脚本自建向量库(纯函数保证与官方产物一致)
- 路线 2:GitHub Action 在每次 merge 时自动 schema 校验 + 全量重建索引 + 附到 Release(天然 CDN + 版本可回滚),消费端下载即用

**消费端**:复用 A 的 MCP server。agent 编码涉及某库某 API 时自动检索已知坑(检索式防呆,AI 时代新工作流)。

**先例**:GitHub Advisory Database(开源 git 仓库承载全球漏洞库,机器人提交+人工 review)、unsight.dev。

**已识别风险**:
- 冷启动:开源项目 99% 死于发布后无第二贡献者;对策是 AI 生产线先产出 80% 初稿
- AI 抽取幻觉会以"看似合理的卡片"污染库,比抽不出更危险
- 时效性:库修复后经验过期,需版本元数据过滤
- 数据许可:issue 内容授权需处理(CC-BY-SA + 溯源链接)
- 模型进化会吃掉部分价值(但垂直库的溯源/新鲜/版本过滤长期存在)

## 四、竞品现状(2026-09 调研结论)

- **官方**:duplicate detection(preview,单仓库、创建时刻、仅 3 条)、官方 MCP server 的 issue 搜索仍是关键词
- **unsight.dev**(136★,Nuxt 团队负责人 Daniel Roe 个人项目):embedding + k-means 聚类 + duplicate 检测 + 浏览器扩展,实际主要索引 nuxt 系仓库
- **小项目群**(2-12★):issue 查重 Action ×2、FAISS 语义搜索 MCP、竞品需求分析 skill——全部个位数星,无一个做完整
- **企业侧**:voice-of-customer 聚类 SaaS(Enterpret/Viable 类)商业成立,但数据不出域的企业进不去——自托管开源无替代
- 结论:需求真实性有多重独立验证;供给端远未饱和;该组合(检索+聚类+晨报+agent 接口+故障库)无人做完整

## 五、两项目关系与执行顺序

A 是引擎(同步管道、embedding、混合检索、MCP、LLM 抽取器),B 是放大应用(A 的第 N 个消费面 + 社区飞轮)。顺序:先 A 后 B。A 的验证方法:1 天脏版(手动索引 1 个活跃仓库 + 最简 MCP)自用一周。

## 六、请评估者回答的问题

1. 需求真实性:上述痛点与价值假设是否成立?有无被高估?
2. 可行性:技术路线与成本估算是否靠谱?致命技术风险?
3. 竞争与时机:官方挤压与 unsight 等先例的威胁判断?窗口期?
4. 失败模式:最可能导致两个项目死亡的具体路径?
5. 结论:各自打分(0-10)并给出「值得做/不值得做/有条件值得做」+ 最关键的一条建议。
